import { SIDEBAR_VISIBILITY,NAVBAR_COLLAPSE} from './../constants';

export const  sidebarVisibility = () => ({ type: SIDEBAR_VISIBILITY});
export const  navbarCollapse = () => ({type: NAVBAR_COLLAPSE});
  
